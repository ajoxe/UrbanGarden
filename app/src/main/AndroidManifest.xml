<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.urbangarden">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/leaflogodarkergreen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/leaflogodarkergreen"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GardenSearchActivity"
            android:theme="@style/ActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GardenDetailActivity"
            android:theme="@style/ActionBar"/>
        <activity android:name=".MyGardensActivity"
            android:theme="@style/ActionBar"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".location.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".userloginandregister.LoginActivity"
            android:theme="@style/ActionBar"/>
        <activity android:name=".userloginandregister.RegisterActivity"
            android:theme="@style/ActionBar"/>
        <activity android:name=".userloginandregister.SettingsActivity"
            android:theme="@style/ActionBar"/>
        <activity android:name=".userloginandregister.MyPostsActivity"
            android:theme="@style/ActionBar"/>
        <activity android:name=".userloginandregister.UserActivity"
            android:theme="@style/ActionBar"></activity>
    </application>

</manifest>